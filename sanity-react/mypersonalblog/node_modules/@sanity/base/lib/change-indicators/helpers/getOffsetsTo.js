"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _isScrollContainer = _interopRequireDefault(require("./isScrollContainer"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var getOffsetsTo = (source, target) => {
  var el = source;
  var bounds = {
    top: 0,
    height: Number.MAX_SAFE_INTEGER
  };
  var top = 0;
  var left = 0;
  var foundScrollContainer = false;

  while (el && el !== target) {
    if (foundScrollContainer) {
      bounds.top += el.offsetTop;
    }

    if ((0, _isScrollContainer.default)(el)) {
      bounds.top = el.offsetTop;
      bounds.height = el.offsetHeight;
      foundScrollContainer = true;
    }

    top += el.offsetTop - el.scrollTop;
    left += el.offsetLeft - el.scrollLeft;
    el = el.offsetParent;
  }

  return {
    top,
    left,
    bounds: {
      top: bounds.top,
      bottom: bounds.top + bounds.height
    }
  };
};

var _default = getOffsetsTo;
exports.default = _default;