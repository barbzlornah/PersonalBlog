import * as React from 'react';
export declare type Direction = 'n' | 'ne' | 'e' | 'se' | 's' | 'sw' | 'w' | 'nw';
declare type Props = {
    wingLength: number;
    length: number;
    top: number;
    left: number;
    direction: number | Direction;
} & Omit<React.ComponentProps<'path'>, 'length' | 'top' | 'left' | 'd'>;
export declare function Arrow({ wingLength, length, top, left, direction, ...rest }: Props): JSX.Element;
export {};
