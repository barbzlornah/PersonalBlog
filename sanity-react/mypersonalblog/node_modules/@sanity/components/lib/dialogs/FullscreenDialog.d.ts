import React from 'react';
import { DialogAction } from './types';
interface FullScreenDialogProps {
    cardClassName?: string;
    className?: string;
    title?: React.ReactNode;
    children?: React.ReactNode;
    onClose?: () => void;
    onEscape?: () => void;
    isOpen?: boolean;
    onAction?: (action: DialogAction) => void;
    actions?: DialogAction[];
    color?: 'default' | 'warning' | 'info' | 'success' | 'danger';
    padding?: 'none' | 'small' | 'medium' | 'large';
}
export default class FullScreenDialog extends React.PureComponent<FullScreenDialogProps> {
    createButtonFromAction: (action: DialogAction, i: number) => JSX.Element;
    renderActions: (actions: DialogAction[]) => JSX.Element | null;
    render(): JSX.Element;
}
export {};
